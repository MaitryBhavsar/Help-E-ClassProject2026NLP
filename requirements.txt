# Core runtime
requests>=2.32
jsonschema>=4.26
networkx>=3.5
pyyaml>=6.0
pydantic>=2.8

# Interactive UI (help_e.ui.server)
fastapi>=0.115
uvicorn>=0.30

# v8 RAG retriever (sentence-transformers MiniLM + MMR).
# Required if you run --system v8.
sentence-transformers>=2.2
numpy>=1.26

# Plot regeneration for analysis/v7_vs_v8/
matplotlib>=3.7

# Optional extras (uncomment if you want them)
# wandb>=0.25                # experiment tracking
# nltk>=3.9                  # auxiliary text utilities
